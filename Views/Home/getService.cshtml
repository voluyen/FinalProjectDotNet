@model List<FinalProjectDotNet.Models.EF.Service>

<div class="services" id="services">
    <div class="container">
        <div class="tittle_head_w3ls mb-5">
            <h3 class="tittle">Services</h3>
        </div>
        <div class="row" style="height:300px;">
            @foreach (var i in Model)
            {
                <div class="col-md-2">
                    <div class="services-left-top">
                        <h5> @i.name </h5>
                    </div>
                </div>
            }
        </div>
    </div>
</div>