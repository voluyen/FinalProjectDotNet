@model List<FinalProjectDotNet.Models.EF.Follow>


<ul>
    @foreach (var i in Model)
    {
        <li><a href="@i.link"><img src="@i.icon" alt="@i.type" style="width:42px;height:42px;"></a></li>
    }
</ul>

